# Stub: STORY-002 — full implementation in EPIC-07
name: Adversary Review
on:
  schedule:
    - cron: '0 10 * * 0'
  workflow_dispatch:
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "stub"
