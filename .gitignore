# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# Node (for scripts/)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
pnpm-lock.yaml

# Rust (for future WASM crates in Phase 4)
target/
Cargo.lock

# Test artifacts
.bats-tmp/
*.bats.log

# Obsidian (if a developer opens this repo in Obsidian)
.obsidian/workspace*
.obsidian/cache
.obsidian/plugins/*/data.json

# Worktrees (per-story git worktrees — not committed, local only)
.worktrees/

# Brain runtime state (this repo is the plugin, not a brain vault — but defense in depth)
.brain/logs/*.jsonl
.brain/cycles/*/health-*.md
.brain/cycles/*/lint-*.md

# Factory state lives on orphan factory-artifacts branch, mounted at .factory/ via git worktree
/.factory/
